[package]
name = "example-hello"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
teletel = { path = "../../teletel", features = ["colors", "minitel2", "serial-terminal", "strict", "tcp-terminal"] }
